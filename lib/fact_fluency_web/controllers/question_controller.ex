defmodule FactFluencyWeb.QuestionController do
    use FactFluencyWeb, :controller

    def answer(conn, %{"questions" => questions}) do
        
    end

    def answer(conn, %{"questions" => questions, "current" => current}) do

    end

end 